local ac game:GetService("Players"). LocalPlayer.PlayerScripts: FindFirstChildOfClass("Actor") if not (ac) then return error("[gunmods error]: unable to find actor.") end

if run on actor or run_on_thread then

(run on actor or run on thread) (ac, [[ getgenv().FOV = true fov enabled (just noticed they have same name too lazy too fix) getgenv().fov = 180 getgenv().VisibleCheck fov size false this right now getgenv().TargetHitbox "Head" little bit buggy sos dont recommend using "Head" or "Torso"

loadstring(game: HttpGet("https://api.mopshub.xyz/scripts/frontlines/silentaim"))()

else 11)

error("executor not supported. (try awp.gg executor)") end
